{
  "name": "decimen-optical-transfer",
  "private": true,
  "version": "0.21.5",
  "license": "MIT",
  "description": "Proof of concept: transfer any file between two devices using fountain-coded animated QR codes, screen to camera, no network.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "dev:tauri": "TAURI_DEV=1 vite --host 127.0.0.1",
    "tauri": "tauri",
    "test": "esbuild tests/protocol.test.ts tests/gallery.test.ts tests/roi.test.ts tests/params.test.ts tests/color.test.ts --bundle --platform=node --outdir=tests/.build --out-extension:.js=.cjs --log-level=error && node --test tests/.build/protocol.test.cjs tests/.build/gallery.test.cjs tests/.build/roi.test.cjs tests/.build/params.test.cjs tests/.build/color.test.cjs",
    "build:pages": "PAGES_DEMO=1 tsc && PAGES_DEMO=1 vite build",
    "e2e:pages": "node e2e-demo.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/qrcode": "^1.5.5",
    "@vitejs/plugin-basic-ssl": "^1.2.0",
    "qrcode": "^1.5.4",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "zxing-wasm": "^2.0.0"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  }
}
